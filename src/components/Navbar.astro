---

---

<script>
  document.addEventListener("DOMContentLoaded", function () {
    function handleClick() {
      window.scrollTo({
        top: 0,
        behavior: "smooth", // Desplazamiento suave
      });
    }

    const homeLink = document.querySelector("#logo");
    if (homeLink) {
      homeLink.addEventListener("click", handleClick);
    }
  });
  document.addEventListener("DOMContentLoaded", function () {
    function handleClick() {
      window.scrollTo({
        top: 0,
        behavior: "smooth", // Desplazamiento suave
      });
    }

    const homeLink = document.querySelector("#tomas");
    if (homeLink) {
      homeLink.addEventListener("click", handleClick);
    }
  });

  document.addEventListener("DOMContentLoaded", function () {
    function handleClick() {
      window.scrollTo({
        top: 0,
        behavior: "smooth", // Desplazamiento suave
      });
    }

    const homeLink = document.querySelector("#inicio");
    if (homeLink) {
      homeLink.addEventListener("click", handleClick);
    }
  });

  document.addEventListener("DOMContentLoaded", function () {
    function handleClick() {
      window.scrollTo({
        top: 1630,
        behavior: "smooth", // Desplazamiento suave
      });
    }

    const homeLink = document.querySelector("#galeria");
    if (homeLink) {
      homeLink.addEventListener("click", handleClick);
    }
  });

  document.addEventListener("DOMContentLoaded", function () {
    function handleClick() {
      window.scrollTo({
        top: 900,
        behavior: "smooth", // Desplazamiento suave
      });
    }

    const homeLink = document.querySelector("#about");
    if (homeLink) {
      homeLink.addEventListener("click", handleClick);
    }
  });

  document.addEventListener("DOMContentLoaded", function () {
    function handleClick() {
      window.scrollTo({
        top: 2500,
        behavior: "smooth", // Desplazamiento suave
      });
    }

    const homeLink = document.querySelector("#contacto");
    if (homeLink) {
      homeLink.addEventListener("click", handleClick);
    }
  });
</script>

<header class="header">
  <div class="logoContainer">
    <img
      id="logo"
      class="logo"
      src="/Tomas-Flores-Actor-Portfolio/caras2rw.webp"
      alt=""
    />
    <span id="tomas">TOMÁS <strong>FLORES</strong></span>
  </div>
  <ul class="ul">
    <li id="inicio">
      {" "}
      Inicio
    </li>
    <li id="about">Sobre mi</li>
    <li id="galeria">Galeria</li>
    <li id="contacto">Contacto</li>
  </ul>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    width: 100%;
    background-color: #060606;
    height: 65px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 10%;
  }

  .logoContainer {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 15px;
  }

  .logoContainer span {
    font-size: 30px;
    font-family: system-ui;
    font-style: italic;
    color: white;
    font-weight: 700;
    cursor: pointer;
  }

  .logoContainer span strong {
    font-weight: 700;
    color: #7a0c1e;
  }

  .logo {
    width: 45px;
    cursor: pointer;
  }

  .cara2 {
    width: 45px;
    cursor: pointer;
    position: absolute;
    top: 0;
    right: 0;
    animation: efecto auto linear;
    animation-timeline: scroll(root block);
  }

  @keyframes efecto {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .ul {
    display: flex;
    list-style: none;
    gap: 50px;
    font-size: 18px;
    color: whitesmoke;
  }

  .ul li {
    cursor: pointer;
    transition: color ease 0.2s;
  }

  .ul li:hover {
    color: #7a0c1e;
  }

  @media screen and (max-width: 900px) {
    .logoContainer span {
      font-size: 15px;
      font-weight: 700;
    }

    .ul {
      display: flex;
      list-style: none;
      gap: 15px;
      font-size: 12px;
      color: whitesmoke;
    }
  }

  @media screen and (max-width: 500px) {
    .header {
      padding: 0 5%;
    }
  }
</style>
